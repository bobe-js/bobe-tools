代码转义

1. bobe

   ````nginx
   h2 text="测试"
   input value={value} oninput={oninput} onkeyup={onkeyup}
   ${Deep} deep=20
   for arr; item i; item.id
     div style="display: flex; align-items: center;"
       h1 onclick={() => delItem(i)}
         {item.value}
       input type="text"  oninput={(e) => updateItem(i, e.target.value)} 
   if show    
     div
       {'哈哈哈'}  
   
   ````

2. 转义后

   ```tsx
   export class MyClass {
     arr = [{ value: '', id: 1 }];
     show = true;
     value = '';
     onkeyup = (e) => {};
     oninput = (e) => {};
     delItem = (i) => {};
     updateItem = (i, value) => {};
   }
   
   class Deep {
     deep = 10;
   }
   
   const _self: MyClass = null as any;
   
   const { value, oninput, onkeyup, arr, delItem, show, updateItem } = _self;
   let h!: typeof document.createElement;
   let t!: typeof document.createTextNode;
   
   function ast() {
     let a0 = h('h2');
     a0.textContent = '测试';
     let a1 = h('input');
     a1.value = value;
     a1.oninput = oninput;
     a1.onkeyup = onkeyup;
     let a2 = new Deep();
     a2.deep = 20;
     arr.forEach((item, i) => {
       let key = item.id;
       let a4 = h('div');a4.style = 'display: flex; align-items: center;';
       let a5 = h('h1');a5.onclick = () => delItem(i);
       let a6 = t(''); a6.textContent = item.value;
       let a7 = h('input');a7.type = 'text'; a7.oninput = (e) => updateItem(i, e.target?.value);
     });
     if (show) {
       let a8 = h('div');
       let a9 = t(''); a9.textContent = '哈哈';
     }
   }
   ```

3. 具体操作

   1. 将 class 拷贝

4. 考虑将 class 整体拷贝